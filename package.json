{
  "name": "broccoli-yaml-to-json",
  "version": "0.0.1",
  "description": "A small description of broccoli-yaml-to-json",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && broccoli build dist",
    "build:serve": "broccoli serve",
    "test": "npm run build && mocha dist/tests --reporter tap",
    "debug-test": "npm run build && node --nolazy --debug-brk=5858 ./node_modules/mocha/bin/mocha dist/tests --reporter tap",
    "test:lint": "npm run build && mocha dist/tests/lint.js",
    "test:debug": "npm run build && mocha debug dist/tests",
    "test:watch": "testem",
    "prepublish": "npm run build"
  },
  "keywords": [
    "broccoli",
    "plugin"
  ],
  "author": "",
  "license": "MIT",
  "repository": {},
  "homepage": "",
  "files": [
    "dist/",
    "!dist/tests"
  ],
  "devDependencies": {
    "broccoli": "^1.1.0",
    "broccoli-babel-transpiler": "^5.6.0",
    "broccoli-cli": "^1.0.0",
    "broccoli-lint-eslint": "^3.2.0",
    "broccoli-merge-trees": "^1.1.4",
    "broccoli-stew": "^1.4.0",
    "chai": "^3.5.0",
    "chai-files": "^1.4.0",
    "es6-promise": "^4.0.5",
    "fixturify": "^0.3.0",
    "fs-extra": "^2.0.0",
    "mocha": "^3.2.0",
    "mocha-eslint": "^3.0.1",
    "regenerator-runtime": "^0.10.1",
    "testem": "^1.13.0",
    "walk-sync": "^0.3.1"
  },
  "dependencies": {
    "broccoli-plugin": "^1.2.1",
    "fs-tree-diff": "^0.5.2",
    "heimdalljs": "^0.3.2",
    "heimdalljs-logger": "^0.1.7",
    "js-yaml": "^3.7.0"
  }
}
